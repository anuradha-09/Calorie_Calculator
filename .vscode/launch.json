{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome with http-server",
            "url": "http://127.0.0.1:8080",
            "webRoot": "${workspaceFolder}",
           
        }
    ]
}

